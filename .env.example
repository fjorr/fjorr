# Cloudflare Image Resizing for media.fjorr.com (width + format=auto).
# Do NOT set until Image Resizing is enabled on the media.fjorr.com zone —
# /cdn-cgi/image/... currently 404s without that product.
# NEXT_PUBLIC_CF_IMAGE_RESIZE=true
