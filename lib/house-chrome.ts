/** Shared house chrome sizing — nav + footer + sheet column. */
export const HOUSE_CHROME_PX = 54;

/** Top white nav margin — scrolls away; glass pill then sticks alone. */
export const NAV_BAND_PX = 54;

/**
 * Full-bleed chrome row — same horizontal padding as the sheet column
 * so titles line up under the wordmark.
 */
export const HOUSE_CHROME_ROW =
  'relative flex h-[54px] w-full items-center px-5 md:px-10';

/** Content column under the wordmark (sheets). Same padding as nav. */
export const HOUSE_CHROME_COLUMN =
  'relative h-full w-full px-5 md:px-10';
